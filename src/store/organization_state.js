export default {
    namespaced: true,
    state: {
      organizations=[]
    },
    mutations: {},
    actions: {
      CreateOrganization({ commit }) {
  
      },
      GetOrgCerts({ commit }) {
  
      },
      AddOrgUser({ commit }) {
  
      },
      GetOrgCertCount({ commit }) {
  
      },
      AddOrgCertCount({ commit }) {
  
    }
    },
  
  }
  